{
  "name": "@shopping-assistant/scraping",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python -m uvicorn src.main:app --reload --port 8000",
    "migrate": "python -c \"from src.db import init_db; init_db(); print('Migrations OK')\"",
    "test": "python -m pytest tests -q",
    "lint": "python -m ruff check src tests",
    "typecheck": "python -m mypy --explicit-package-bases --follow-imports=skip src/intent/ollama.py src/normalization/engine.py src/scoring/engine.py src/estimation/engine.py"
  }
}
